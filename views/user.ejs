<!-- userPage.ejs -->

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Page</title>
    <style>
      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f0f0f0;
        margin: 0;
        padding: 0;
        display: flex;
        flex-direction: column;
        min-height: 100vh;
      }

      header {
        background-color: #3498db;
        color: #fff;
        text-align: center;
        padding: 1rem 0;
      }

      main {
        flex: 1;
        margin: 20px;
      }

      h1,
      h2 {
        color: #3498db;
      }

      ul {
        list-style: none;
        padding: 0;
        margin: 0;
      }

      li {
        margin-bottom: 20px;
        border: 1px solid #ddd;
        border-radius: 8px;
        padding: 15px;
        background-color: #fff;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }

      footer {
        background-color: #3498db;
        color: #fff;
        text-align: center;
        padding: 1rem 0;
      }
    </style>
  </head>
  <body>
    <header>
      <h1>User Page</h1>
    </header>
    <main>
      <h2>Admin Messages</h2>

      <% if (adminMessages.length > 0) { %>
      <ul>
        <% adminMessages.forEach(messages => { %>
        <li>
          <strong>Name:</strong> <%= messages.name %><br />
          <strong>Email:</strong> <%= messages.email %><br />
          <strong>Message:</strong> <%= messages.message %>
        </li>
        <% }); %>
      </ul>
      <% } else { %>
      <p>No admin messages available.</p>
      <% } %> cosnt response =

      <form
        id="contactForm"
        action="/contact-uss/6gdfghj"
        method="post"
      >
        <label for="name">Name:</label>
        <input type="text" id="name" name="name" required />

        <label for="email">Email:</label>
        <input type="email" id="email" name="email" required />

        <label for="message">Message:</label>
        <textarea id="message" name="message" required></textarea>

        <button type="submit">Submit</button>
      </form>
    </main>
    <footer>
      <p>&copy; 2023 Your Company. All rights reserved.</p>
      <!-- Add more footer content as needed -->
    </footer>
  </body>
</html>
